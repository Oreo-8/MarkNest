# MarkNest - 本地 Markdown 阅读与编辑器 需求说明

## 1. 项目概述

MarkNest 是一款面向 Chrome 和 Edge 的本地 Markdown 阅读、编辑与文件导航扩展。用户直接在浏览器中打开 `.md` 或 `.markdown` 文件时，扩展在原始 `file://` 地址页面内提供目录、预览、编辑、搜索、代码高亮、图片预览和保存能力，文件内容不上传到网络。

## 2. 产品目标

- 提供开箱即用的本地 Markdown 阅读体验。
- 在阅读模式与编辑模式之间快速切换。
- 保留真实本地文件地址，支持相对文档链接和相对图片。
- 所有核心能力离线运行，不依赖服务器。
- 保持界面简洁、深色主题友好，并适合长文档阅读。

## 3. 运行环境

- Google Chrome，支持 Manifest V3。
- Microsoft Edge，支持 Manifest V3。
- 用户需在扩展详情页开启“允许访问文件网址”。
- 支持 `.md` 和 `.markdown` 文件。
- 主要适配桌面端，最小页面宽度为 760px。

## 4. 功能需求

### 4.1 本地文件接管

- 用户在浏览器中打开本地 Markdown 文件后，扩展自动渲染阅读界面。
- 地址栏必须保留原始 `file://` 文件地址，不跳转到扩展内部地址。
- 点击扩展工具栏图标时，显示快捷悬浮窗，可新建独立的空白编辑会话。
- 空白会话默认展开编辑器；打开已有文件时默认进入阅读模式。

### 4.2 Markdown 预览

- 支持标题、段落、粗体、斜体、删除线、引用、列表、任务列表、表格、分隔线、行内代码和代码块。
- 支持 HTTP/HTTPS 链接、页内锚点和本地相对 Markdown 链接。
- 点击相对 Markdown 链接时，浏览器打开对应本地文件，并继续由 MarkNest 接管。
- 支持网络图片和本地相对图片。
- 图片按预览区域自适应显示。
- 点击图片打开全屏大图预览；点击背景、关闭按钮或按 `Esc` 关闭。

### 4.3 目录树

- 根据文档的 H1–H6 标题生成目录树。
- 点击目录项可滚动定位到对应标题。
- 预览滚动时，目录自动跟随并高亮当前章节。
- 目录支持收起和展开。
- 目录字体固定为紧凑的 13px 系统无衬线字体。

### 4.4 目录搜索

- 点击目录区域的搜索图标后，在目录上方显示搜索框。
- 输入关键字后，只显示标题中包含关键字的目录项。
- 搜索不区分英文大小写。
- 显示“匹配数量 / 总数量”。
- 清空搜索内容后恢复完整目录。
- 按 `Esc` 清空搜索并收起搜索框。

### 4.5 编辑器

- 编辑器默认可收起，通过顶部铅笔图标展开或收起。
- 编辑内容实时更新 Markdown 预览。
- 编辑器滚动时，预览按文档进度同步滚动。
- 预览与编辑器之间提供可拖动分隔条，用于调整区域宽度。
- 双击分隔条恢复默认比例。
- 编辑栏显示行数和字符数。
- 支持 Tab 键插入两个空格。

### 4.6 文件保存

- 提供“保存源文件”和“另存为”图标按钮。
- 首次保存源文件时允许用户选择原文件并授予写入权限。
- 获得文件句柄授权后，后续保存可直接覆盖源文件。
- 支持 `Ctrl/Command + S` 保存源文件。
- 支持 `Ctrl/Command + Shift + S` 另存为。
- 底部保存快捷键提示只在编辑器展开时显示。

### 4.7 状态恢复

- 自动保存当前文件名、编辑内容和未保存状态。
- 页面刷新后恢复未保存的编辑内容。
- 已打开文件与空白编辑会话相互独立。
- 主题、预览字号、编辑器宽度和内容居中状态需要持久化。

### 4.8 阅读外观

- 默认使用黑色主题，支持黑白主题切换。
- 黑色主题保持近黑背景，不改变既定色彩风格。
- 强调色采用蓝紫色。
- 调色板图标打开悬浮外观面板。
- 外观面板提供 12–22px 的预览字号滑块。
- 字体选择功能当前暂不提供。
- 提供内容居中图标，在铺满显示和居中阅读宽度之间切换。

### 4.9 代码块

- 使用内置 Highlight.js 离线进行语法高亮。
- 黑白主题分别提供合适的代码块背景和高亮颜色。
- 代码块不显示逐行背景色。
- 右上角显示语言名称；悬停后切换为复制图标。
- 点击复制图标复制完整代码，并显示成功状态。

### 4.10 返回顶部

- 预览向下滚动超过约 260px 后，在预览区右下角显示返回顶部图标。
- 点击后平滑滚动到预览顶部。
- 回到顶部后按钮自动隐藏。

### 4.11 文件拖放

- 支持将 `.md` 或 `.markdown` 文件拖入界面打开。
- 拖动文件进入页面时显示明确的接收提示。
- 不支持的文件类型需要给出提示，不加载文件。

### 4.12 AI 对话收录

- 插件悬浮窗提供“收录对话”功能，在 ChatGPT、Gemini、DeepSeek、通义千问、Grok、豆包、Kimi 和智谱清言对话页面可用。
- 自动识别当前 AI 平台，读取用户与 AI 消息，并转换为带平台名称、来源链接和收录时间的结构化 Markdown。
- 收录时弹出系统保存位置选择框，用户确认后保存 `.md` 文件，并在 MarkNest 阅读界面中打开。
- 用户取消位置选择时，不保存文件，也不打开新阅读页。
- 对话内容仅在本地浏览器内处理，不上传到第三方服务器。

## 5. 界面要求

- 顶部左侧显示目录开关和当前文件名。
- 顶部右侧依次提供外观、内容居中、主题和编辑器开关。
- 目录、预览和编辑区域具有清晰层级，但不使用多余标题文字。
- 所有主要操作使用图标，悬停时通过 `title` 显示功能名称。
- 悬浮面板锚点必须与触发图标中心对齐。
- 默认深色界面使用近黑背景与蓝紫强调色。

## 6. 图标与品牌

- 产品名为 `MarkNest - 本地 Markdown 阅读与编辑器`，简称为 `MarkNest`。
- 图标采用近黑圆角底和蓝紫渐变文档/编辑符号。
- 图标外部背景透明。
- 扩展提供 16、32、48 和 128px PNG 图标。
- 发布素材保留 1024px PNG 主图。

## 7. 权限与安全

- 扩展使用 Manifest V3。
- 申请本地存储、本地文件接管、当前标签页脚本执行和 Markdown 下载所需权限。
- AI 网站访问权限仅覆盖已支持平台的官方网页域名，只在用户主动点击“收录对话”时读取当前页面。
- Markdown 内容只在本地浏览器中处理，不上传服务器。
- 外部链接使用安全的跳转属性。
- 用户必须主动开启“允许访问文件网址”后，扩展才能接管本地文件。
- 未开启本地文件访问时，扩展图标显示警示徽标；打开 MarkNest 时显示设置引导，并在用户返回后自动重新检测。

## 8. 非功能需求

- 打开普通 Markdown 文件后应在可感知的短时间内完成渲染。
- 长文档滚动、目录高亮和编辑同步应保持流畅。
- 所有功能在离线状态下可用，网络图片除外。
- 界面在黑白主题下均需保证文字和控件对比度。
- 功能按钮应提供可访问名称，支持键盘操作。
- 使用 WXT + Vue 3 + TypeScript + Pinia + `@webext-core/messaging` 开发，并生成可直接作为“已解压扩展”加载的 `.output/chrome-mv3/` 产物。

## 9. 验收标准

- 打开本地 Markdown 后地址栏仍为真实文件地址。
- README 中的相对设计文档链接能够打开目标 Markdown。
- 本地相对图片能够显示，并可点击打开大图预览。
- 目录搜索只显示匹配项，清空后恢复全部目录。
- 编辑、滚动同步、宽度拖动、保存和刷新恢复均正常。
- 代码高亮、代码复制、主题、字号、内容居中和返回顶部均可用。
- 扩展管理页面正确显示 MarkNest 名称、版本和图标。

## 10. 发布物

- `src/`：WXT + Vue 3 + TypeScript + Pinia 扩展源代码。
- `public/`：扩展图标等无需编译的静态资源，Manifest 由 WXT 生成。
- `.output/chrome-mv3/`：可直接加载的扩展生产构建；`pnpm release` 同时生成 `.output/marknest-2.3.0-chrome.zip`。
- `assets/marknest-icon-1024.png`：发布主图标。
- `MarkNest-需求说明.md`：项目需求与验收依据。
